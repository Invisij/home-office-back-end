<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Read user page</title>
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
            crossorigin="anonymous"
        />
    </head>
    <body>
        <div class="container">
            <div class="row mt-5">
                <h2>Read user</h2>
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Email</th>
                            <th scope="col">Password</th>
                            <th scope="col">Role</th>
                            <th scope="col">First name</th>
                            <th scope="col">Last name</th>
                            <th scope="col"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% data.users.map((item,index) => { %>
                        <tr>
                            <th scope="row"><%=index+1%></th>
                            <td><%=item.email%></td>
                            <td><%=item.password%></td>
                            <td><%=item.role%></td>
                            <td><%=item.firstName%></td>
                            <td><%=item.lastName%></td>
                            <td>
                                <a href="./edit-user?id=<%=item.id%>" type="button" class="btn btn-outline-success">
                                    Edit
                                </a>
                                <a href="./delete-user?id=<%=item.id%>" type="button" class="btn btn-outline-danger">
                                    Delete
                                </a>
                            </td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </body>
</html>
